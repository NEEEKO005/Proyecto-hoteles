[project]
name = "proyecto-hoteles"
version = "0.1.0"
description = "Sistema de administración de habitaciones de hotel."
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "radon>=6.0.1",
    "ruff>=0.15.5",
    "pytest>=9.0.2",
]
